# Top-5 Basic V2 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Turn the current one-click notebook into a compact real implementation of the supplied Top-5 training code.

**Architecture:** Preserve the current public APIs around data, training, decoding, and submissions. Replace only the model core with tested pre-norm RMSNorm/RoPE/GQA/SwiGLU blocks and add direction-token/length-filter preprocessing.

**Tech Stack:** Python, PyTorch, SentencePiece, SacreBLEU, pandas, tqdm, Google Colab T4.

## Global Constraints

- Default to full training on the complete dataset.
- Use no pretrained model and no external training data.
- Keep the implementation readable and contained in `nlp_basic.py`.
- Do not add contrastive or bidirectional training.

---

### Task 1: Top-5 core blocks

**Files:** Modify `nlp_basic.py`; modify `tests/test_nlp_basic.py`.

**Interfaces:** Produce `RMSNorm`, `RoPE`, `apply_rope`, `GroupedQueryAttentionRoPE`, `SwiGLU`, `EncoderLayer`, and `DecoderLayer`.

- [ ] Add tests for normalization and attention/layer shapes; run them and confirm missing imports fail.
- [ ] Implement the smallest pre-norm blocks with padding and causal masks.
- [ ] Run the focused tests and confirm they pass.

### Task 2: Full translation model and preprocessing

**Files:** Modify `nlp_basic.py`; modify `tests/test_nlp_basic.py`.

**Interfaces:** Extend `ModelConfig` with `n_kv_heads` and `rope_base`; keep `Seq2SeqTransformer.encode`, `decode`, and `forward`; produce `add_direction_token` and `filter_pairs_by_token_length`.

- [ ] Add failing tests for `<2vi>`, length filtering, GQA model forward, and tied weights.
- [ ] Replace the standard Transformer core and apply the direction token in training/inference.
- [ ] Run the focused and full unit suites.

### Task 3: One-click full Colab workflow

**Files:** Modify `OlpAI_NLP_Fast_Contest.ipynb`, `README.md`, and `tests/test_notebook_contract.py`.

**Interfaces:** Default `SMOKE_TEST=False`; full configuration is 256/8/4/4+4/768; existing output filenames remain unchanged.

- [ ] Add failing notebook contract assertions for full-mode defaults and latest-repo pull.
- [ ] Update setup, tokenizer filtering, model config, mode banner, warnings, and documentation.
- [ ] Compile all cells and run the entire test suite.

### Task 4: Publish and Colab run

**Files:** No production file changes unless runtime testing reveals a reproduced defect.

- [ ] Inspect the diff and scan for secrets and local paths.
- [ ] Run all tests and Python compilation fresh.
- [ ] Commit and push `main`.
- [ ] Reload the GitHub-backed Colab notebook and run until full-data/model configuration is visibly confirmed.
